import { getPortfolioPage } from '../../portfolioPage.js';

export default await getPortfolioPage({ basePath: '../../', publicationId: 'pulse/symbiote-workspace-workspace-presentation-journey-v1' });
