import { getPortfolioPage } from '../../portfolioPage.js';

export default await getPortfolioPage({ basePath: '../../', publicationId: 'pulse/native-encoded-segment-compatibility-concat-planning' });
