import { getPortfolioPage } from '../../../../portfolioPage.js';

export default await getPortfolioPage({ basePath: '../../../../', publicationId: 'pulse/real-time-capacity-admission-control-pre-dispatch' });
